// ウィンドウオプション（タイトル・サイズ）を指定してWebView表示
設定 = {"タイトル": "マイアプリ", "サイズ": [800, 600]}

HtmlText = 『<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: sans-serif; padding: 24px; background: #1e1e2e; color: #cdd6f4; text-align: center; }
    h1 { color: #f38ba8; }
    button { padding: 10px 24px; font-size: 16px; background: #89b4fa; color: #111; border: none; border-radius: 6px; cursor: pointer; font-weight: bold; }
    button:hover { background: #b4befe; }
    #msg { margin-top: 16px; font-size: 18px; color: #a6e3a1; }
  </style>
</head>
<body>
  <h1>🌸 なでしこ3 WebViewウィンドウ</h1>
  <p>サイズ800x600のWebViewウィンドウを起動しました。</p>
  <button id="myBtn">ボタンをクリック</button>
  <div id="msg"></div>
  <script>
    document.getElementById('myBtn').onclick = function() {
      document.getElementById('msg').innerText = 'クリックされました！ (' + new Date().toLocaleTimeString() + ')';
    };
  </script>
</body>
</html>』

設定でHtmlTextのウィンドウ作成。
