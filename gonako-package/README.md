# gonako-package

なでしこのパッケージの入口は `<package-name>.nako3` をこのディレクトリに直接配置します。
関連ファイルは `<package-name>/` 以下へ配置します。
各入口のファイル名が名前空間になるため、パッケージ間で一意の名前を使用してください。
このディレクトリはGoのビルド時にランタイムへ埋め込まれます。
詳細は [パッケージの仕様](../docs/gonako-package.md) を参照してください。
