# なでしこ3 Go版 (gonako) 高速化・改善検討メモ

本ドキュメントは、`gonako` のVMインタプリタ実行およびコンパイラ・ランタイムにおけるボトルネックの分析と、今後の高速化・性能改善の候補施策をまとめたものです。

---

## 1. 現在のVMボトルネック内訳 (プロファイル分析)

`internal/vm/bench_test.go`（`BenchmarkLoop` 等）のプロファイルから判明している主なコスト構成は以下の通りです。

| 費用の源泉 | 全体割合 | 主な要因・詳細 |
|---|---|---|
| **命令ディスパッチ** | **約40%** | `execute` ループ内の `switch inst.Op` による分岐予測ミスおよびジャンプのオーバーヘッド |
| **スタック push / pop** | **約23%** | 56バイトある `value.Value` 構造体の値コピーが毎命令ごとに発生 |
| **演算処理・型チェック** | **約15%** | `ops.Binary` 呼び出し、数値判定 `Number()`、四則演算等の実行 |
| **変数セル読み書き** | **約10%** | `*value.Cell` ポインタ経由の間接アクセス、定数書き換え防止チェック |
| **暴走防止カウンタ** | **約2%** | 毎命令ごとの `m.executed++` および上限値チェック |

---

## 2. 実施済みの施策

1. **定数畳み込み (`fold.go`)**:
   - コンパイル時に直列な定数式を評価し `OpLoadConst` に集約。
2. **IR覗き穴最適化とスーパー命令 (`peephole.go` / `OpBinaryAt`)**:
   - `Load; Load; Binary` を 1命令の `OpBinaryAt` に融合（3ディスパッチ・4スタック操作 → 1ディスパッチ・1push）。
   - ループ1周あたりの命令数を 19命令 → 13命令 に削減（`BenchmarkLoop` -18%）。
3. **関数の呼び出しアロケーション削減 (`vm.go: callClosure`)**:
   - 呼び出し1回あたりの確保を 5個 → 3個（フレーム、localsスライス、セルのまとめ確保）に削減。
   - スタック事前確保（`MaxStack`）、引数のゼロコピー借用（`borrowN`）。
   - `BenchmarkRecursion` -21%, `BenchmarkCalls` -17%。
4. **`ir.Inst` の縮小 (40B → 20B, int32化)** (PR #19):
   - オペランド `A`, `B`, `C`, `Pos` を `int` から `int32` に変更。
   - 64バイトのCPUキャッシュラインに収まる命令数が 1命令 → 3命令 に向上。
   - `BenchmarkRecursion` -5.6%, `BenchmarkCalls` -4.1% 高速化。
5. **スーパー命令 `OpBinaryAtStoreLocal` (PR #20)**:
   - `Load; Load; Binary; StoreLocal`（4命令）を1命令に融合。
   - ループカウンタ更新等のスタック push/pop を完全にバイパス。
6. **スーパー命令 `OpJumpIfBinaryAt` / `OpJumpIfNotBinaryAt` (PR #20)**:
   - `Load; Load; Binary; JumpIfFalse/True`（4命令）を1命令に融合。
   - ループ条件・比較分岐のスタック push/pop および真偽値オブジェクト生成を完全バイパス。
7. **スーパー命令 `OpBinaryStoreLocal` / `OpBinaryStoreGlobal` (PR #20)**:
   - `Binary; StoreLocal/Global`（2命令）を1命令に融合。
   - 式の計算結果をスタックを経ずに直接セルへ書き込み。
8. **スーパー命令 `OpStoreSoreAndLocal` / `OpStoreSoreAndGlobal` (PR #20)**:
   - `LoadLocal; Dup; StoreSpecial; StoreLocal/Global`（4命令）を1命令に融合。
   - 回数ループや範囲ループの毎周のループ変数代入オーバーヘッドを 4命令 → 1命令に圧縮。
9. **スーパー命令 `OpIndexGetAt` (PR #20)**:
   - `Load; Load; IndexGet 1`（3命令）を1命令に融合。
   - 配列要素 `A[I]` を直読みし `arr.Get(i)` から直接プッシュ。
   - **成果**: `BenchmarkLoop` が 28.5ms → **20.0ms** (**-29.8% 高速化**)。総合ベンチマーク合計 2200.0ms → **1948.4ms** (**-11.4%、Node.jsの1.58倍高速**)。Sieve（エラトステネスの篩）で **Node.js 公式実装を逆転**（82.6ms vs 97.2ms）。
10. **`value.Value` の縮小 (56B → 32B, Issue #21)**:
   - 文字列・配列・辞書・関数が別々に持っていた参照領域を、GCが追跡する単一のポインタへ集約。
   - 文字列はポインタとUTF-8バイト長を保持し、生成時の追加ヒープ確保を避ける。
   - 変更前後のテストバイナリを交互に8ラウンド測定した中央値で、`BenchmarkLoop` **-73.8%**、`BenchmarkRecursion` **-41.9%**、`BenchmarkCalls` **-47.3%**。
   - `allocs/op` は同等のまま、`BenchmarkRecursion` の `B/op` は15.77MB→10.86MB、`BenchmarkCalls` は58.91MB→41.31MBに減少。

---

## 3. 今後の改善施策候補

### 施策A: さらなるスーパー命令（命令融合）
* **推定効果**: 特定ベンチマークで **3〜5%** 改善
* **実装難易度**: **小**
* **内容**:
  - `Binary; Return`（式評価結果を直接 return）の融合（再帰関数の末尾等）。
  - 配列代入 `Load; Load; Load; IndexSet` → `OpIndexSetAt` の融合。

### 施策C: 非エスケープ（Leaf）関数のフレーム・セル再利用
* **推定効果**: `BenchmarkRecursion` / `BenchmarkCalls` で **10〜20%** 改善
* **実装難易度**: **中**
* **内容**:
  * 現在は関数呼び出しごとに 3アロケーション（`frame` 構造体、`locals` スライス、`Cell` 配列）が発生している。
  * クロージャを作らない（`NumCaptures == 0` かつ `OpMakeFunc` を呼ばない）末端の関数呼び出しでは、呼び出し終了後にセルやフレームを再利用できる（フリーリスト / オブジェクトプール `sync.Pool` 等の活用）。
  * アロケーションが 0 になれば、再帰処理（Fibonacci 等）の実行速度が大きく向上する。

### 施策D: 命令カウンタ・暴走検知チェックのバッチ化
* **推定効果**: ホットループで **1〜2%** 改善
* **実装難易度**: **小**
* **内容**:
  * 現在は 1命令実行ごとに `m.executed++` と `m.executed > maxInstructions` の比較を行っている。
  * 基本ブロック（Jump や分岐がない直線的な命令列）ごとに命令数をまとめて `m.executed += N` するか、バックエッジ（`OpJump` で上に戻るループ分岐）でのみチェックするように変更することで、命令間のオーバーヘッドを削減する。

### 施策E: gogen（Goネイティブ生成）のさらなる最適化
* **推定効果**: gogen 実行時で **10〜30%** 改善
* **実装難易度**: **中**
* **内容**:
  * 現在は `float64` のみを型推論・特殊化している。
  * ループカウンタなどの確定整数演算、あるいはローカルな文字列バッファ操作・配列確保の事前サイズ推論（Pre-allocation）を強化することで、Goコンパイラ（gc）の最適化（インライン化・エスケープ解析）をさらに引き出す。

---

## 4. 検討の上で見送った施策

### × VM側への型推論の導入
* **検討結果**: **不採用（費用対効果が低い）**
* **理由**:
  * gogenで型推論が効くのは、「56バイトのValueボックス化の排除」「Goローカル変数へのスタック平坦化」「直接Go演算子への展開」がセットで効くため。
  * VMでは値スタックとセルの構造が必須であるため、型推論で削れるのは `ops.Binary` 内の型判定分岐（全体の15%中の一部）のみにとどまり、推定効果は 5% 前後。
  * 一方でIR命令セットの肥大化、コンパイラ・検証器・gogenへの影響など実装・保守コストが大きすぎる。
