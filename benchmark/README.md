# なでしこ3 動作速度ベンチマーク

本家リポジトリ（TypeScript / Node.js 公式実装 `cnako3`）と、Go言語による本実装 `gonako`（バイトコードVM実行）、および Goコード生成バックエンド `gogen`（Goネイティブコンパイル実行）の動作速度を比較測定したベンチマーク結果です。

## 1. 測定環境

- **OS**: macOS (darwin/arm64, Apple Silicon)
- **Go バージョン**: go version go1.26.5 darwin/arm64
- **Node.js バージョン**: v24.18.0
- **測定日**: 2026-09-03
- **測定方法**: 各テストプログラムをウォームアップ後に 5 回実行し、平均実行時間を算出

## 2. 比較対象

| 対象 | 実行方式 | 特徴 |
|---|---|---|
| **cnako3 (本家)** | `node src/cnako3.mjs` | 公式 TypeScript 実装。Node.js / V8 JIT ランタイム上で動作 |
| **gonako (VM実行)** | `bin/gonako <file>` | 本実装のスタック型バイトコードインタプリタ。Goネイティブバイナリ |
| **gogen (Goネイティブ)** | `gonako gengo` → `go build` | なでしこプログラムをGoソースに変換し、ネイティブコンパイルして実行 |

## 3. ベンチマークテスト一覧

代表的なアルゴリズム（再帰・配列・数値計算・ソート・文字列・連想配列）を網羅したテストセットです。

| No | テスト名 | プログラム | アルゴリズム概要 / 計算規模 | 計算結果（全環境一致確認） |
|---|---|---|---|---|
| 1 | **Fibonacci (再帰呼び出し)** | [`01_fibonacci.nako3`](./01_fibonacci.nako3) | 再帰呼び出しとスタック処理の性能 (fib(30)) | `fib(30) = 832040` |
| 2 | **Sieve (エラトステネスの篩)** | [`02_sieve.nako3`](./02_sieve.nako3) | 配列アクセス・更新とループ処理 (20万までの素数列挙) | `sieve(200000) = 17984` |
| 3 | **Mandelbrot (マンデルブロ集合)** | [`03_mandelbrot.nako3`](./03_mandelbrot.nako3) | 浮動小数点演算と多重ループ (150x150グリッド反復計算) | `mandelbrot(150x150) = 683844` |
| 4 | **QuickSort (クイックソート)** | [`04_quicksort.nako3`](./04_quicksort.nako3) | 配列要素比較・スワップと分割統治 (10,000要素のソート) | `quicksort(10000) = len:10000, first:97856, mid:1052817920, last:2147470080` |
| 5 | **Collatz (コラッツ予想)** | [`05_collatz.nako3`](./05_collatz.nako3) | 整数演算・条件分岐・反復処理 (1〜3万の探索) | `collatz(30000) = num:26623, max_step:307` |
| 6 | **String (文字列処理)** | [`06_string.nako3`](./06_string.nako3) | 文字列連結・置換・検索・抽出 (3,000回反復) | `string(3000) = total_len:445778, final_len:107` |
| 7 | **Dict (連想配列/辞書操作)** | [`07_dict.nako3`](./07_dict.nako3) | 辞書へのキー値挿入・参照・集計 (3万回操作) | `dict(30000) = sum:450015000` |

## 4. ベンチマーク測定結果

| ベンチマーク項目 | cnako3 (Node.js) | gonako (VM) | gogen (Goネイティブ) | gonako速度比 (対cnako3) | gogen速度比 (対cnako3) |
|---|---|---|---|---|---|
| **Fibonacci (再帰呼び出し)** | 1938.3 ms | 735.4 ms | **743.5 ms** | 2.64x | **2.61x** |
| **Sieve (エラトステネスの篩)** | 87.3 ms | 100.3 ms | **109.0 ms** | 0.87x | **0.80x** |
| **Mandelbrot (マンデルブロ集合)** | 191.0 ms | 291.6 ms | **293.8 ms** | 0.65x | **0.65x** |
| **QuickSort (クイックソート)** | 90.7 ms | 54.4 ms | **57.4 ms** | 1.67x | **1.58x** |
| **Collatz (コラッツ予想)** | 211.4 ms | 655.8 ms | **627.8 ms** | 0.32x | **0.34x** |
| **String (文字列処理)** | 65.3 ms | 12.6 ms | **14.2 ms** | 5.18x | **4.60x** |
| **Dict (連想配列/辞書操作)** | 65.3 ms | 26.9 ms | **28.0 ms** | 2.43x | **2.33x** |
|---|---|---|---|---|---|
| **合計 (Total)** | **2649.3 ms** | **1877.0 ms** | **1873.7 ms** | **1.41x** | **1.41x** |

※ 速度比（倍率）は `cnako3の所要時間 / 対象の所要時間` です（1.00x より大きいほど高速）。

## 5. 結果の考察と分析

### ① Go版が本家より速いところ

**起動が軽い。** Goのネイティブ単一バイナリなので、Node.jsプロセスの初期化とTypeScriptパーサーの読み込みがありません。数十msで終わるテスト（String・Dict）では、この差がそのまま順位になります。

**文字列と辞書が速い。** Goネイティブの `string` (UTF-8) と、挿入順を保つ辞書の実装が効きます。特に文字列処理は本家の5倍前後です。

**関数呼び出しが速い。** 再帰（Fibonacci）は本家の2.6倍前後で、これが合計を押し上げている最大の要因です。呼び出し1回あたりの割り当てを3個まで減らしてあります（AGENTS.md 6節）。

### ② Go版が本家より遅いところ

**数値ループはV8に負けています。** Collatz・Mandelbrot・Sieve は本家より遅く、特にCollatzは3倍ほどの差があります。これは実装の詰めが甘いというより、方式の差です。

- V8はJITで、ホットな数値ループを型を特殊化したマシン語に落とします。Go版のVMはバイトコードインタプリタで、JITを持ちません。
- 数値は float64 一本で、値は `Value` に包んだまま扱います。「この変数は整数だ」と決めつける最適化はしません。JavaScriptの意味論を曲げないための方針です（AGENTS.md 12節）。
- したがって、この差を埋めるには型推論か実行時の特殊化が要ります。互換性を優先している間は、ここは本家に譲る前提で読んでください。

### ③ gogen (Goネイティブ生成) は今のところVMと大差ありません

ネイティブコンパイルする以上もっと速くなりそうに見えますが、実測では合計でVMとほぼ同じです。Collatzのように演算が続くものだけ数%〜10%ほど勝ちます。

理由は、現在のgogenが「バイトコードの機械的なGoソース化」だからです（docs/gogen.md 7節）。命令ディスパッチは消えますが、オペランドスタックの push/pop はそのまま残り、値の演算も `internal/vm` へ委譲します。式を直接Goの式へ組み立てる作りに変えれば伸びしろがありますが、それは今の構造を変えることになります。

裏を返すと、**gogenを使う理由は現状では速度よりも配布性**（Goツールチェインのある環境でネイティブ実行ファイルを作れること）にあります。

### ④ 表の読み方の注意

**日をまたいだ比較には使えません。** この表の絶対値は測定した日のマシンの状態に強く依存します。実際、同じ7ケースでcnako3の合計が 3236.9ms の日と 2646.9ms の日があり、Go版を1行も変えていなくても20%近く動きます。

変更の前後を比べたいときは、**変更前後のバイナリを同じマシンで1回ずつ交互に回し、その中央値を取る**こと。片方をまとめて測ってからもう片方を測ると、熱による速度低下の分だけ結果を読み違えます。次節がその方法で取った値です。

## 6. 最適化の履歴

この節の数字は、前節の表とは別に、変更前後のバイナリを交互に5回ずつ回して中央値を取ったものです。

### 定数伝播とIRの覗き穴最適化 (2026-09-03)

`internal/compiler/fold.go`（定数伝播）と `internal/compiler/peephole.go`（覗き穴最適化・スーパー命令 `OpBinaryAt`）を入れたときの効果です。

| ベンチマーク項目 | gonako VM 前 | gonako VM 後 | 差 | gogen 前 | gogen 後 | 差 |
|---|---:|---:|---:|---:|---:|---:|
| Fibonacci | 793 ms | 749 ms | **-5.5%** | 777 ms | 748 ms | -3.7% |
| Sieve | 117 ms | 104 ms | **-11.1%** | 119 ms | 114 ms | -4.2% |
| Mandelbrot | 323 ms | 297 ms | **-8.0%** | 314 ms | 300 ms | -4.5% |
| QuickSort | 62 ms | 57 ms | **-8.1%** | 60 ms | 61 ms | +1.7% |
| Collatz | 768 ms | 668 ms | **-13.0%** | 714 ms | 640 ms | -10.4% |
| String | 15 ms | 15 ms | ±0 | 15 ms | 15 ms | ±0 |
| Dict | 30 ms | 29 ms | -3.3% | 30 ms | 29 ms | -3.3% |

演算とループが主体のもの（Collatz・Sieve・Mandelbrot）ほど効き、命令呼び出しが主体のもの（String）は変わりません。`Load;Load;Binary` を1命令にまとめる最適化なので、素直な結果です。

`internal/vm/bench_test.go` の合成ケースでも同じ傾向で、Loop -18%、Calls -11%、Recursion -5% でした。

## 7. ベンチマークの再実行方法

以下のコマンドで、すべてのテストのビルド、測定、および本ドキュメントの再生成が自動で行われます。

```bash
# gonako本体をビルド
make cmd

# ベンチマークの自動実行
go run ./benchmark/runner.go
```

本ドキュメントは `runner.go` が全体を生成します。**README.md を直接編集しても次の実行で消える**ので、文章を足すときは `runner.go` の生成部を直してください。
