# なでしこ3 動作速度ベンチマーク

本家リポジトリ（TypeScript / Node.js 公式実装 `cnako3`）と、Go言語による本実装 `gonako`（バイトコードVM実行）、Goコード生成バックエンド `gogen`（Goネイティブコンパイル実行）、さらに比較の物差しとして同じアルゴリズムを素の **Node.js (JavaScript)** と **Python3** で書いたものを並べて測定した結果です。

## 1. 測定環境

- **OS**: darwin/arm64
- **Go バージョン**: go version go1.26.5 darwin/arm64
- **Node.js バージョン**: v24.18.0
- **Python バージョン**: Python 3.13.9
- **測定日**: 2026-09-11
- **測定方法**: 各テストプログラムをウォームアップ後に 5 回実行し、平均実行時間を算出

## 2. 比較対象

| 対象 | 実行方式 | 特徴 |
|---|---|---|
| **cnako3 (本家)** | `node src/cnako3.mjs` | 公式 TypeScript 実装。Node.js / V8 JIT ランタイム上で動作 |
| **gonako (VM)** | `bin/gonako <file>` | 本実装のスタック型バイトコードインタプリタ。Goネイティブバイナリ |
| **gogen (Goネイティブ)** | `gonako gengo` → `go build` | なでしこプログラムをGoソースに変換し、ネイティブコンパイルして実行 |
| **Node.js** | `node benchmark/js/<file>.js` | 同じアルゴリズムを素のJavaScriptで書いたもの。言語そのものの速度の目安 |
| **Python3** | `python3 benchmark/py/<file>.py` | 同じアルゴリズムを素のPythonで書いたもの。言語そのものの速度の目安 |

Node.js版は [`js/`](./js)、Python3版は [`py/`](./py) にあります。どちらも `.nako3` と**同じアルゴリズム・同じ計算規模**で書いてあり、出力文字列も一致します。なでしこ（と本家cnako3）の数値は倍精度浮動小数点なので、Python版のうち擬似乱数を使う QuickSort だけは、同じ値を得るためにあえて `math.fmod` で浮動小数点演算に揃えてあります。

## 3. ベンチマークテスト一覧

代表的なアルゴリズム（再帰・配列・数値計算・ソート・文字列・連想配列）を網羅したテストセットです。

| No | テスト名 | プログラム | アルゴリズム概要 / 計算規模 | 計算結果（全環境一致確認） |
|---|---|---|---|---|
| 1 | **Fibonacci (再帰呼び出し)** | [`01_fibonacci.nako3`](./01_fibonacci.nako3) / [js](./js/01_fibonacci.js) / [py](./py/01_fibonacci.py) | 再帰呼び出しとスタック処理の性能 (fib(30)) | `fib(30) = 832040` |
| 2 | **Sieve (エラトステネスの篩)** | [`02_sieve.nako3`](./02_sieve.nako3) / [js](./js/02_sieve.js) / [py](./py/02_sieve.py) | 配列アクセス・更新とループ処理 (20万までの素数列挙) | `sieve(200000) = 17984` |
| 3 | **Mandelbrot (マンデルブロ集合)** | [`03_mandelbrot.nako3`](./03_mandelbrot.nako3) / [js](./js/03_mandelbrot.js) / [py](./py/03_mandelbrot.py) | 浮動小数点演算と多重ループ (150x150グリッド反復計算) | `mandelbrot(150x150) = 683844` |
| 4 | **QuickSort (クイックソート)** | [`04_quicksort.nako3`](./04_quicksort.nako3) / [js](./js/04_quicksort.js) / [py](./py/04_quicksort.py) | 配列要素比較・スワップと分割統治 (10,000要素のソート) | `quicksort(10000) = len:10000, first:97856, mid:1052817920, last:2147470080` |
| 5 | **Collatz (コラッツ予想)** | [`05_collatz.nako3`](./05_collatz.nako3) / [js](./js/05_collatz.js) / [py](./py/05_collatz.py) | 整数演算・条件分岐・反復処理 (1〜3万の探索) | `collatz(30000) = num:26623, max_step:307` |
| 6 | **String (文字列処理)** | [`06_string.nako3`](./06_string.nako3) / [js](./js/06_string.js) / [py](./py/06_string.py) | 文字列連結・置換・検索・抽出 (3,000回反復) | `string(3000) = total_len:445778, final_len:107` |
| 7 | **Dict (連想配列/辞書操作)** | [`07_dict.nako3`](./07_dict.nako3) / [js](./js/07_dict.js) / [py](./py/07_dict.py) | 辞書へのキー値挿入・参照・集計 (3万回操作) | `dict(30000) = sum:450015000` |

## 4. ベンチマーク測定結果

### 4.1 実行時間

| ベンチマーク項目 | cnako3 (本家) | gonako (VM) | gogen (Goネイティブ) | Node.js | Python3 |
|---|---:|---:|---:|---:|---:|
| **Fibonacci (再帰呼び出し)** | 2175.6 ms | 181.5 ms | 89.7 ms | **27.3 ms** | 189.2 ms |
| **Sieve (エラトステネスの篩)** | 94.8 ms | 32.2 ms | **20.7 ms** | 23.7 ms | 130.0 ms |
| **Mandelbrot (マンデルブロ集合)** | 203.2 ms | 78.3 ms | **17.8 ms** | 26.4 ms | 165.0 ms |
| **QuickSort (クイックソート)** | 97.4 ms | 23.5 ms | **18.6 ms** | 24.0 ms | 127.0 ms |
| **Collatz (コラッツ予想)** | 217.3 ms | 296.7 ms | 130.9 ms | **25.7 ms** | 276.5 ms |
| **String (文字列処理)** | 75.5 ms | **16.1 ms** | 19.4 ms | 21.7 ms | 122.2 ms |
| **Dict (連想配列/辞書操作)** | 71.1 ms | 18.7 ms | **15.9 ms** | 24.7 ms | 126.2 ms |
| **合計 (Total)** | **2934.9 ms** | **647.0 ms** | **313.0 ms** | **173.5 ms** | **1136.2 ms** |

### 4.2 速度比（本家 cnako3 を 1.00x としたとき）

| ベンチマーク項目 | cnako3 (本家) | gonako (VM) | gogen (Goネイティブ) | Node.js | Python3 |
|---|---:|---:|---:|---:|---:|
| **Fibonacci (再帰呼び出し)** | 1.00x | 11.99x | 24.25x | 79.62x | 11.50x |
| **Sieve (エラトステネスの篩)** | 1.00x | 2.95x | 4.58x | 4.00x | 0.73x |
| **Mandelbrot (マンデルブロ集合)** | 1.00x | 2.59x | 11.42x | 7.70x | 1.23x |
| **QuickSort (クイックソート)** | 1.00x | 4.14x | 5.23x | 4.07x | 0.77x |
| **Collatz (コラッツ予想)** | 1.00x | 0.73x | 1.66x | 8.45x | 0.79x |
| **String (文字列処理)** | 1.00x | 4.70x | 3.90x | 3.48x | 0.62x |
| **Dict (連想配列/辞書操作)** | 1.00x | 3.80x | 4.46x | 2.88x | 0.56x |
| **合計 (Total)** | **1.00x** | **4.54x** | **9.38x** | **16.92x** | **2.58x** |

※ 速度比（倍率）は `cnako3の所要時間 / 対象の所要時間` です（1.00x より大きいほど高速）。

## 5. 結果の考察と分析

### ① Go版が本家より速いところ

**起動が軽い。** Goのネイティブ単一バイナリなので、Node.jsプロセスの初期化とTypeScriptパーサーの読み込みがありません。数十msで終わるテスト（String・Dict・QuickSort）では、この差がそのまま順位になります。

**文字列と辞書が速い。** Goネイティブの `string` (UTF-8) と、挿入順を保つ辞書の実装が効きます。

**関数呼び出しが速い。** 再帰（Fibonacci）の差がもっとも大きく、これが合計を押し上げている最大の要因です。呼び出し1回あたりの割り当てを3個まで減らしてあります（docs/parser.md）。

### ② VM実行が本家より遅いところ

**数値ループはV8のJITに負けます。** 残っている弱点はCollatzで、ここだけは本家より遅いままです。V8はホットな数値ループを型を特殊化したマシン語に落としますが、`gonako` のVMはバイトコードインタプリタで、JITを持ちません。素のNode.js列と見比べると差の出どころがはっきりします。

値の持ち方も効いています。VMは値を `value.Value` に包んだまま扱い、演算のたびに包み直します。ここを詰めたのが次の gogen です。

### ③ gogen (Goネイティブ生成) は数値計算で本家を追い越します

`internal/gogen/types.go` の型推論により、生成コードは**数値と証明できた場所を生の `float64` で計算します**。オペランドスタックはGoのローカル変数に、捕捉されない数値ローカルはただの `float64` 変数になり、`rt.Binary(...)` は `f0 = f1 + f2` になります。

この結果、計算集約のケースでgogenがVMを大きく引き離します（同一マシンでの前後比較は次節）。

推論できないところは今までどおり `rt.Value` のまま一般経路を通ります。**証明できたときだけ特殊化する**方針なので、当てが外れても遅くなるだけで、結果は変わりません（docs/gogen.md）。

なお、掛け算だけは生成コードで `float64(a * b)` と明示的に丸めています。これがないとarm64などでGoコンパイラが直後の加減算とまとめてFMA命令に融合し、中間結果が丸められず、JavaScript（とVM実行）と答えが変わってしまうためです。変換自体に実行時コストはありません。

逆に、文字列処理（String）や辞書操作（Dict）はもともと命令呼び出しが主体で、数値演算がほとんどないため、gogenにしてもVMとあまり変わりません。

### ④ 素のNode.js・Python3との位置関係

Node.js と Python3 の列は、**言語処理系そのものの地力**を示す物差しです。なでしこの列と直接勝ち負けを競わせるためのものではありません（なでしこ側は日本語の構文解析とプラグイン命令の呼び出しを通るぶん、同じ計算でも手数が増えます）。

読み方の目安は次のとおりです。

- **20ms前後はプロセス起動のぶん**です。`node` も `python3` も、何もしないスクリプトで20ms台かかります。数十msで終わるテスト（String・Dict・QuickSort）でネイティブバイナリの `gogen` が勝つのは、主にこの起動コストの差です。
- **Node.js (素のJS) が強いのは数値ループと再帰**です。V8のJITが効く領域で、Fibonacci と Collatz では `gogen` もまだ届きません。逆に配列・文字列・辞書が主体のケースでは、起動コストを含めると `gogen` が上回ります。
- **Python3 は全体に重め**です。`gonako` のVMはバイトコードインタプリタという点ではCPythonと同じ方式ですが、値表現がGoの構造体で固定サイズ・ヒープ割り当てが少ないぶん有利で、全ケースで上回っています。
- **cnako3 と 素のNode.js の差**が、そのまま「なでしこ処理系の上乗せ分」の目安になります。同様に `gonako` と `gogen` の差が、Go版におけるVM実行のオーバーヘッドです。

### ⑤ 表の読み方の注意

**日をまたいだ比較には使えません。** この表の絶対値は測定した日のマシンの状態に強く依存します。実際、同じ7ケースでcnako3の合計が 3236.9ms の日と 2646.9ms の日があり、Go版を1行も変えていなくても20%近く動きます。

変更の前後を比べたいときは、**変更前後のバイナリを同じマシンで1回ずつ交互に回し、その中央値を取る**こと。片方をまとめて測ってからもう片方を測ると、熱による速度低下の分だけ結果を読み違えます。次節がその方法で取った値です。

## 6. 最適化の履歴

この節の数字は、前節の表とは別に、変更前後のバイナリを交互に5回ずつ回して中央値を取ったものです。

### gogenの型推論と非ボックス化 (2026-09-03)

`internal/gogen/types.go` を入れ、生成コードのオペランドスタックをGoの変数へ展開し、数値と証明できた値を生の `float64` で持つようにしたときの効果です（VM実行は変えていないので数字も動きません）。

| ベンチマーク項目 | gogen 前 | gogen 後 | 差 |
|---|---:|---:|---:|
| Fibonacci | 740 ms | 541 ms | **-26.9%** |
| Sieve | 114 ms | 57 ms | **-50.0%** |
| Mandelbrot | 300 ms | 51 ms | **-83.0%** |
| QuickSort | 61 ms | 37 ms | **-39.3%** |
| Collatz | 637 ms | 162 ms | **-74.6%** |
| String | 14 ms | 15 ms | +7.1% |
| Dict | 29 ms | 24 ms | **-17.2%** |

数値演算が主体のもの（Mandelbrot・Collatz・Sieve）ほど効きます。String がわずかに動いているのは測定誤差の範囲で、文字列処理には数値がほとんど出てこないため、推論しても変わりません。

### 定数伝播とIRの覗き穴最適化 (2026-09-03)

`internal/compiler/fold.go`（定数伝播）と `internal/compiler/peephole.go`（覗き穴最適化・スーパー命令 `OpBinaryAt`）を入れたときの効果です。

| ベンチマーク項目 | gonako VM 前 | gonako VM 後 | 差 | gogen 前 | gogen 後 | 差 |
|---|---:|---:|---:|---:|---:|---:|
| Fibonacci | 793 ms | 749 ms | **-5.5%** | 777 ms | 748 ms | -3.7% |
| Sieve | 117 ms | 104 ms | **-11.1%** | 119 ms | 114 ms | -4.2% |
| Mandelbrot | 323 ms | 297 ms | **-8.0%** | 314 ms | 300 ms | -4.5% |
| QuickSort | 62 ms | 57 ms | **-8.1%** | 60 ms | 61 ms | +1.7% |
| Collatz | 768 ms | 668 ms | **-13.0%** | 714 ms | 640 ms | -10.4% |
| String | 15 ms | 15 ms | ±0 | 15 ms | 15 ms | ±0 |
| Dict | 30 ms | 29 ms | -3.3% | 30 ms | 29 ms | -3.3% |

演算とループが主体のもの（Collatz・Sieve・Mandelbrot）ほど効き、命令呼び出しが主体のもの（String）は変わりません。`Load;Load;Binary` を1命令にまとめる最適化なので、素直な結果です。

`internal/vm/bench_test.go` の合成ケースでも同じ傾向で、Loop -18%、Calls -11%、Recursion -5% でした。

## 7. ベンチマークの再実行方法

以下のコマンドで、すべてのテストのビルド、測定、および本ドキュメントの再生成が自動で行われます。

```bash
# gonako本体をビルド
just cmd

# ベンチマークの自動実行
go run ./benchmark/runner.go
```

`node` や `python3` が見つからない環境では、その処理系の列だけを飛ばして測定します（本家 `cnako3` の実行には Node.js が必要です）。

本ドキュメントは `runner.go` が全体を生成します。**README.md を直接編集しても次の実行で消える**ので、文章を足すときは `runner.go` の生成部を直してください。
